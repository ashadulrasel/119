<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
    <title>𝕃𝕚𝕧𝕖 𝕊𝕥𝕣𝕖𝕒𝕞 - Premium Live TV</title>
    
    <style>
        /* ==========================================
           CSS ভেরিয়েবল - কালার ম্যানেজমেন্ট
        ========================================== */
        :root {
            --primary: #667eea;
            --secondary: #764ba2;
            --accent: #f5576c;
            --glass-bg: rgba(255, 255, 255, 0.1);
            --glass-border: rgba(255, 255, 255, 0.2);
        }

        /* রিসেট */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
            font-size: 16px;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            background-image: url('https://images.unsplash.com/photo-1583409430135-1ccd865c940e?q=80&w=1170');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            min-height: 100vh;
            color: white;
            overflow-x: hidden;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(10px);
            z-index: -1;
        }

        /* ==========================================
           লোডিং স্ক্রিন
        ========================================== */
        .loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 10000;
            transition: all 0.5s;
        }

        .loading-screen.hidden {
            opacity: 0;
            visibility: hidden;
            pointer-events: none;
        }

        .loading-logo {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            border: 5px solid rgba(255, 255, 255, 0.3);
            animation: rotate 3s linear infinite;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }

        @keyframes rotate {
            0% { transform: rotate(0deg) scale(1); }
            50% { transform: rotate(180deg) scale(1.1); }
            100% { transform: rotate(360deg) scale(1); }
        }

        .loading-text {
            margin-top: 30px;
            font-size: 2.5rem;
            font-weight: 800;
            color: white;
            text-shadow: 0 5px 30px rgba(0, 0, 0, 0.5);
            animation: glow 2s ease-in-out infinite;
        }

        @keyframes glow {
            0%, 100% { opacity: 1; transform: translateY(0); }
            50% { opacity: 0.8; transform: translateY(-10px); }
        }

        /* ==========================================
           নেভবার
        ========================================== */
        .navbar {
            background: var(--glass-bg);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--glass-border);
            padding: 1rem 2rem;
            position: sticky;
            top: 0;
            z-index: 1000;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .nav-container {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .brand {
            display: flex;
            align-items: center;
            gap: 1rem;
            cursor: pointer;
            transition: transform 0.3s;
        }

        .brand:hover {
            transform: scale(1.05);
        }

        .brand-logo {
            width: 55px;
            height: 55px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid rgba(255, 255, 255, 0.3);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.6);
        }

        .brand-text {
            font-size: 1.8rem;
            font-weight: 800;
            background: linear-gradient(45deg, #fff, #a8edea);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .nav-links {
            display: flex;
            gap: 1rem;
            align-items: center;
            list-style: none;
        }

        .nav-links a, .nav-links button {
            color: white;
            text-decoration: none;
            padding: 0.7rem 1.2rem;
            border-radius: 12px;
            background: var(--glass-bg);
            border: 1px solid var(--glass-border);
            cursor: pointer;
            transition: all 0.3s;
            font-size: 0.95rem;
            font-weight: 600;
            white-space: nowrap;
        }

        .nav-links a:hover, .nav-links button:hover {
            background: rgba(255, 255, 255, 0.25);
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
        }

        .menu-toggle {
            display: none;
            background: var(--glass-bg);
            border: 1px solid var(--glass-border);
            color: white;
            font-size: 1.8rem;
            padding: 0.6rem 1rem;
            border-radius: 12px;
            cursor: pointer;
        }

        /* প্রোফাইল বাটন */
        .profile-section {
            position: relative;
        }

        .profile-btn {
            display: flex;
            align-items: center;
            gap: 0.8rem;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 0.6rem 1.2rem;
            border-radius: 30px;
            cursor: pointer;
            transition: all 0.3s;
            box-shadow: 0 5px 20px rgba(102, 126, 234, 0.4);
        }

        .profile-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 30px rgba(102, 126, 234, 0.6);
        }

        .profile-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 800;
            color: #667eea;
            font-size: 1.1rem;
        }

        .profile-dropdown {
            position: absolute;
            top: 110%;
            right: 0;
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(30px);
            border-radius: 20px;
            padding: 1.5rem;
            min-width: 300px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            opacity: 0;
            visibility: hidden;
            transform: translateY(-20px);
            transition: all 0.3s;
        }

        .profile-dropdown.active {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }

        .profile-info {
            border-bottom: 2px solid rgba(0, 0, 0, 0.1);
            padding-bottom: 1rem;
            margin-bottom: 1rem;
        }

        .profile-info h3 {
            color: #333;
            margin-bottom: 0.5rem;
        }

        .profile-info p {
            color: #666;
            font-size: 0.9rem;
        }

        .profile-badge {
            display: inline-block;
            padding: 0.4rem 1rem;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 700;
            margin-top: 0.5rem;
        }

        .badge-basic {
            background: linear-gradient(135deg, #84fab0 0%, #8fd3f4 100%);
            color: #fff;
        }

        .badge-pro {
            background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
            color: #fff;
        }

        .badge-ultra {
            background: linear-gradient(135deg, #30cfd0 0%, #330867 100%);
            color: #fff;
        }

        .logout-btn {
            width: 100%;
            padding: 1rem;
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            color: white;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            font-weight: 700;
            transition: all 0.3s;
        }

        .logout-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(245, 87, 108, 0.4);
        }

        /* ==========================================
           নোটিশ বার
        ========================================== */
        .notice-bar {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            padding: 1rem 0;
            overflow: hidden;
        }

        .notice-content {
            display: flex;
            align-items: center;
            animation: scroll 25s linear infinite;
            white-space: nowrap;
            gap: 3rem;
        }

        @keyframes scroll {
            0% { transform: translateX(100%); }
            100% { transform: translateX(-100%); }
        }

        .notice-badge {
            background: white;
            color: #f5576c;
            padding: 0.4rem 1rem;
            border-radius: 20px;
            font-weight: 700;
            font-size: 0.85rem;
        }

        .notice-text {
            color: white;
            font-size: 1rem;
            font-weight: 600;
        }

        /* ==========================================
           স্লাইডার
        ========================================== */
        .banner-slider {
            max-width: 1400px;
            margin: 2rem auto;
            padding: 0 1rem;
        }

        .slider-container {
            position: relative;
            height: 450px;
            border-radius: 25px;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }

        .slide {
            position: absolute;
            width: 100%;
            height: 100%;
            opacity: 0;
            transition: opacity 1s;
        }

        .slide.active {
            opacity: 1;
        }

        .slide img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .slider-nav {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            background: rgba(255, 255, 255, 0.3);
            backdrop-filter: blur(10px);
            border: none;
            color: white;
            font-size: 2rem;
            padding: 1rem 1.3rem;
            cursor: pointer;
            transition: all 0.3s;
            z-index: 10;
        }

        .slider-nav:hover {
            background: rgba(255, 255, 255, 0.5);
        }

        .slider-nav.prev { left: 20px; border-radius: 0 15px 15px 0; }
        .slider-nav.next { right: 20px; border-radius: 15px 0 0 15px; }

        .slider-dots {
            position: absolute;
            bottom: 25px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            gap: 12px;
            z-index: 10;
        }

        .dot {
            width: 14px;
            height: 14px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.5);
            cursor: pointer;
            transition: all 0.3s;
        }

        .dot.active {
            background: white;
            width: 35px;
            border-radius: 7px;
        }

        /* ==========================================
           সেকশন
        ========================================== */
        .section {
            max-width: 1400px;
            margin: 3rem auto;
            padding: 0 1rem;
        }

        .section-title {
            color: white;
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            text-shadow: 0 3px 15px rgba(0, 0, 0, 0.3);
            display: flex;
            align-items: center;
            gap: 1rem;
            font-weight: 800;
        }

        .title-icon {
            width: 12px;
            height: 12px;
            background: red;
            border-radius: 50%;
            animation: blink 1.5s infinite;
        }

        @keyframes blink {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.3; }
        }

        /* ==========================================
           ইভেন্ট গ্রিড
        ========================================== */
        .events-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 1.5rem;
        }

        .event-card {
            background: var(--glass-bg);
            backdrop-filter: blur(20px);
            border-radius: 20px;
            padding: 1.5rem;
            border: 1px solid var(--glass-border);
            cursor: pointer;
            transition: all 0.3s;
            position: relative;
            overflow: hidden;
        }

        .event-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
        }

        .event-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1rem;
        }

        .event-live-badge {
            background: red;
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .event-live-dot {
            width: 10px;
            height: 10px;
            background: white;
            border-radius: 50%;
            animation: blink 1s infinite;
        }

        .event-match {
            color: white;
            font-size: 1.4rem;
            font-weight: 700;
            margin-bottom: 0.8rem;
        }

        .event-time {
            color: rgba(255, 255, 255, 0.8);
            font-size: 1rem;
        }

        /* ==========================================
           ক্যাটাগরি ট্যাব
        ========================================== */
        .category-tabs {
            display: flex;
            gap: 0.8rem;
            margin-bottom: 1.5rem;
            flex-wrap: wrap;
            overflow-x: auto;
            padding-bottom: 0.5rem;
        }

        .category-tab {
            background: var(--glass-bg);
            backdrop-filter: blur(20px);
            color: white;
            border: 1px solid var(--glass-border);
            padding: 0.8rem 1.5rem;
            border-radius: 25px;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: 600;
            white-space: nowrap;
        }

        .category-tab:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-3px);
        }

        .category-tab.active {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-color: transparent;
            box-shadow: 0 5px 20px rgba(102, 126, 234, 0.5);
        }

        /* ==========================================
           চ্যানেল গ্রিড
        ========================================== */
        .channels-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
            gap: 1.5rem;
        }

        .channel-card {
            background: var(--glass-bg);
            backdrop-filter: blur(20px);
            border-radius: 20px;
            padding: 1.2rem;
            border: 1px solid var(--glass-border);
            cursor: pointer;
            transition: all 0.3s;
            text-align: center;
        }

        .channel-card:hover {
            transform: translateY(-8px) scale(1.05);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
        }

        .channel-logo-container {
            width: 100%;
            height: 130px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1rem;
            border-radius: 15px;
            background: rgba(255, 255, 255, 0.05);
            overflow: hidden;
        }

        .channel-logo {
            max-width: 90%;
            max-height: 90%;
            object-fit: contain;
            transition: transform 0.3s;
        }

        .channel-card:hover .channel-logo {
            transform: scale(1.15);
        }

        .channel-name {
            color: white;
            font-weight: 700;
            margin-bottom: 0.8rem;
            font-size: 1.05rem;
        }

        .channel-badges {
            display: flex;
            gap: 0.4rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .badge {
            padding: 0.3rem 0.8rem;
            border-radius: 15px;
            font-size: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
        }

        .badge-live {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            color: white;
        }

        .badge-hd {
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            color: white;
        }

        .badge-pro {
            background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
            color: white;
        }

        .badge-free {
            background: linear-gradient(135deg, #84fab0 0%, #8fd3f4 100%);
            color: white;
        }

        .badge-4k {
            background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%);
            color: #333;
        }

        .badge-b {
            background: linear-gradient(135deg, #a1c4fd 0%, #c2e9fb 100%);
            color: #333;
        }

        .badge-pro-plus {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        /* ==========================================
           প্লেয়ার মডাল
        ========================================== */
        .player-modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            backdrop-filter: blur(20px);
            z-index: 9000;
            display: none;
            align-items: center;
            justify-content: center;
        }

        .player-modal.active {
            display: flex;
        }

        .player-container {
            width: 95%;
            max-width: 1300px;
            background: rgba(20, 20, 20, 0.98);
            backdrop-filter: blur(30px);
            border-radius: 25px;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
        }

        .player-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .player-title {
            color: white;
            font-size: 1.8rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .close-player {
            background: rgba(255, 255, 255, 0.2);
            border: none;
            color: white;
            font-size: 2rem;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            cursor: pointer;
            transition: all 0.3s;
        }

        .close-player:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: rotate(90deg);
        }

        .video-wrapper {
            position: relative;
            width: 100%;
            padding-bottom: 56.25%;
            background: #000;
        }

        #videoPlayer {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }

        .player-brand-overlay {
            position: absolute;
            left: 30px;
            top: 50%;
            transform: translateY(-50%);
            display: flex;
            align-items: center;
            gap: 1rem;
            z-index: 100;
            opacity: 0;
            animation: fadeIn 1s ease-in-out 1.5s forwards;
            pointer-events: none;
        }

        @keyframes fadeIn {
            to { opacity: 0.8; }
        }

        .player-brand-logo {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            object-fit: cover;
            box-shadow: 0 5px 25px rgba(0, 0, 0, 0.6);
        }

        .player-brand-text {
            color: white;
            font-size: 1.8rem;
            font-weight: 800;
            text-shadow: 0 3px 15px rgba(0, 0, 0, 0.8);
        }

        .player-controls {
            background: linear-gradient(to top, rgba(0, 0, 0, 0.9), transparent);
            padding: 1.5rem;
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .control-btn {
            background: var(--glass-bg);
            backdrop-filter: blur(15px);
            border: 1px solid var(--glass-border);
            color: white;
            padding: 0.8rem 1.3rem;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s;
            font-size: 0.95rem;
            font-weight: 600;
        }

        .control-btn:hover {
            background: rgba(255, 255, 255, 0.25);
            transform: translateY(-3px);
        }

        .quality-selector {
            position: relative;
        }

        .quality-options {
            position: absolute;
            bottom: 60px;
            left: 0;
            background: rgba(20, 20, 20, 0.98);
            backdrop-filter: blur(30px);
            border-radius: 15px;
            padding: 0.8rem;
            display: none;
            flex-direction: column;
            gap: 0.4rem;
            min-width: 120px;
        }

        .quality-options.active {
            display: flex;
        }

        .quality-option {
            padding: 0.7rem 1rem;
            color: white;
            cursor: pointer;
            border-radius: 10px;
            transition: all 0.3s;
            font-weight: 600;
        }

        .quality-option:hover {
            background: rgba(255, 255, 255, 0.15);
        }

        /* ==========================================
           লগইন মডাল
        ========================================== */
        .login-modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(30px);
            z-index: 10000;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
        }

        .login-modal.hidden {
            display: none;
        }

        .login-box {
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(50px);
            border-radius: 35px;
            padding: 3rem 2.5rem;
            max-width: 450px;
            width: 100%;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            animation: slideUp 0.5s ease-out;
        }

        @keyframes slideUp {
            from {
                transform: translateY(100px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .login-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .login-logo {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
            margin: 0 auto 1.5rem;
            box-shadow: 0 15px 40px rgba(102, 126, 234, 0.4);
            border: 3px solid rgba(102, 126, 234, 0.3);
        }

        .login-title {
            font-size: 2.2rem;
            color: #333;
            margin-bottom: 0.8rem;
            font-weight: 800;
        }

        .login-subtitle {
            color: #666;
            font-size: 1rem;
        }

        .input-group {
            margin-bottom: 1.5rem;
            position: relative;
        }

        .input-icon {
            position: absolute;
            left: 1.2rem;
            top: 50%;
            transform: translateY(-50%);
            font-size: 1.3rem;
        }

        .login-input {
            width: 100%;
            padding: 1.1rem 1.2rem 1.1rem 3.5rem;
            border: 2px solid #e0e0e0;
            border-radius: 18px;
            font-size: 1.05rem;
            transition: all 0.3s;
            background: rgba(255, 255, 255, 0.9);
        }

        .login-input:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 8px 20px rgba(102, 126, 234, 0.3);
        }

        .login-btn {
            width: 100%;
            padding: 1.2rem;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 18px;
            font-size: 1.15rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s;
            margin-top: 1rem;
        }

        .login-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 30px rgba(102, 126, 234, 0.5);
        }

        .login-error {
            background: #ffe6e6;
            color: #d32f2f;
            padding: 1rem;
            border-radius: 15px;
            margin-top: 1rem;
            text-align: center;
            display: none;
            font-weight: 600;
        }

        .login-error.show {
            display: block;
        }

        /* ==========================================
           M3U আপলোড
        ========================================== */
        .upload-box {
            background: var(--glass-bg);
            backdrop-filter: blur(20px);
            border: 3px dashed var(--glass-border);
            border-radius: 25px;
            padding: 3rem 2rem;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s;
        }

        .upload-box:hover {
            border-color: rgba(255, 255, 255, 0.5);
            background: rgba(255, 255, 255, 0.15);
        }

        .upload-icon {
            font-size: 5rem;
            margin-bottom: 1.5rem;
        }

        .upload-text {
            color: white;
            font-size: 1.3rem;
            margin-bottom: 1.5rem;
            font-weight: 600;
        }

        .upload-btn {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 1rem 2.5rem;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            font-size: 1.1rem;
            font-weight: 700;
            transition: all 0.3s;
        }

        .upload-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 30px rgba(102, 126, 234, 0.6);
        }

        /* ==========================================
           ফুটার
        ========================================== */
        footer {
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(20px);
            padding: 3rem 2rem;
            margin-top: 4rem;
        }

        .footer-content {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2.5rem;
        }

        .footer-section h3 {
            color: white;
            margin-bottom: 1.2rem;
            font-size: 1.5rem;
        }

        .social-links {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .social-btn {
            background: var(--glass-bg);
            color: white;
            padding: 0.8rem 1.3rem;
            border-radius: 25px;
            text-decoration: none;
            transition: all 0.3s;
            border: 1px solid var(--glass-border);
            font-weight: 600;
        }

        .social-btn:hover {
            background: rgba(255, 255, 255, 0.25);
            transform: translateY(-3px);
        }

        .footer-text {
            color: rgba(255, 255, 255, 0.7);
            line-height: 1.8;
        }

        .copyright {
            text-align: center;
            color: rgba(255, 255, 255, 0.6);
            margin-top: 2.5rem;
            padding-top: 1.5rem;
            border-top: 1px solid var(--glass-border);
        }

        /* ==========================================
           হেল্পার ক্লাস
        ========================================== */
        .hidden {
            display: none !important;
        }

        /* ==========================================
           রেসপন্সিভ - মোবাইল
        ========================================== */
        @media (max-width: 768px) {
            .nav-links {
                position: fixed;
                top: 75px;
                left: -100%;
                background: rgba(10, 10, 10, 0.98);
                backdrop-filter: blur(30px);
                width: 85%;
                height: calc(100vh - 75px);
                flex-direction: column;
                padding: 2rem 1.5rem;
                transition: left 0.4s;
                overflow-y: auto;
                align-items: stretch;
            }

            .nav-links.active {
                left: 0;
            }

            .nav-links li {
                width: 100%;
            }

            .nav-links a, .nav-links button {
                display: block;
                width: 100%;
                text-align: left;
            }

            .menu-toggle {
                display: block;
            }

            .brand-text {
                font-size: 1.3rem;
            }

            .slider-container {
                height: 250px;
            }

            .channels-grid {
                grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
                gap: 1rem;
            }

            .events-grid {
                grid-template-columns: 1fr;
            }

            .section-title {
                font-size: 1.8rem;
            }

            .player-brand-overlay {
                left: 15px;
            }

            .player-brand-logo {
                width: 40px;
                height: 40px;
            }

            .player-brand-text {
                font-size: 1.2rem;
            }

            .profile-dropdown {
                right: -50px;
            }
        }

        /* স্ক্রলবার */
        ::-webkit-scrollbar {
            width: 10px;
        }

        ::-webkit-scrollbar-track {
            background: rgba(255, 255, 255, 0.05);
        }

        ::-webkit-scrollbar-thumb {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <!-- লোডিং স্ক্রিন -->
    <div class="loading-screen" id="loadingScreen">
        <img src="https://avatars.githubusercontent.com/u/192325222?v=4" alt="Loading" class="loading-logo">
        <div class="loading-text">𝕃𝕚𝕧𝕖 𝕊𝕥𝕣𝕖𝕒𝕞</div>
    </div>

    <!-- লগইন মডাল -->
    <div class="login-modal" id="loginModal">
        <div class="login-box">
            <div class="login-header">
                <img src="https://avatars.githubusercontent.com/u/192325222?v=4" alt="Logo" class="login-logo">
                <h2 class="login-title">Welcome Back</h2>
                <p class="login-subtitle">Sign in to continue</p>
            </div>
            <form id="loginForm">
                <div class="input-group">
                    <span class="input-icon">👤</span>
                    <input type="text" id="username" class="login-input" placeholder="Username" required>
                </div>
                <div class="input-group">
                    <span class="input-icon">🔒</span>
                    <input type="password" id="password" class="login-input" placeholder="Password" required>
                </div>
                <button type="submit" class="login-btn">Sign In</button>
                <div id="loginError" class="login-error"></div>
            </form>
        </div>
    </div>

    <!-- নেভবার -->
    <nav class="navbar">
        <div class="nav-container">
            <div class="brand" onclick="scrollToTop()">
                <img src="https://avatars.githubusercontent.com/u/192325222?v=4" alt="Logo" class="brand-logo">
                <span class="brand-text">𝕃𝕚𝕧𝕖 𝕊𝕥𝕣𝕖𝕒𝕞</span>
            </div>
            
            <button class="menu-toggle" onclick="toggleMenu()">☰</button>
            
            <ul class="nav-links" id="navLinks">
                <li><a href="#home">🏠 Home</a></li>
                <li><a href="#live-events">🔴 Live Events</a></li>
                <li><a href="#channels">📺 Channels</a></li>
                <li><a href="#m3u-upload">📤 M3U</a></li>
                <li><a href="https://example.com" target="_blank">🔗 Link 1</a></li>
                <li><a href="https://example.com" target="_blank">🔗 Link 2</a></li>
                
                <li class="profile-section hidden" id="profileContainer">
                    <div class="profile-btn" onclick="toggleProfile()">
                        <div class="profile-avatar" id="profileAvatar">U</div>
                        <span id="profileName">User</span>
                    </div>
                    <div class="profile-dropdown" id="profileDropdown">
                        <div class="profile-info">
                            <h3 id="dropdownName">User</h3>
                            <p id="dropdownEmail">user@example.com</p>
                            <span class="profile-badge" id="dropdownBadge">Basic</span>
                            <p style="margin-top: 10px; font-size: 0.85rem; color: #888;" id="dropdownExpiry">Expires: 30 days</p>
                        </div>
                        <button class="logout-btn" onclick="logout()">🚪 Logout</button>
                    </div>
                </li>
            </ul>
        </div>
    </nav>

    <!-- নোটিশ বার -->
    <div class="notice-bar">
        <div class="notice-content">
            <span class="notice-badge">🔔 UPDATE</span>
            <span class="notice-text">Welcome to Premium Live TV! Enjoy HD & 4K channels!</span>
            <span class="notice-badge">📢 NEWS</span>
            <span class="notice-text">Watch live sports, movies & exclusive content! 🎉</span>
        </div>
    </div>

    <!-- স্লাইডার -->
    <div class="banner-slider" id="home">
        <div class="slider-container">
            <div class="slide active">
                <img src="https://images.unsplash.com/photo-1522869635100-9f4c5e86aa37?w=1400" alt="Banner 1">
            </div>
            <div class="slide">
                <img src="https://images.unsplash.com/photo-1560169897227-fe8b57c47bf0?w=1400" alt="Banner 2">
            </div>
            <div class="slide">
                <img src="https://images.unsplash.com/photo-1574375927938-d5a98e8ffe85?w=1400" alt="Banner 3">
            </div>
            
            <button class="slider-nav prev" onclick="changeSlide(-1)">❮</button>
            <button class="slider-nav next" onclick="changeSlide(1)">❯</button>
            
            <div class="slider-dots">
                <span class="dot active" onclick="goToSlide(0)"></span>
                <span class="dot" onclick="goToSlide(1)"></span>
                <span class="dot" onclick="goToSlide(2)"></span>
            </div>
        </div>
    </div>

    <!-- লাইভ ইভেন্ট -->
    <div class="section" id="live-events">
        <h2 class="section-title">
            <span class="title-icon"></span>
            Live Events
        </h2>
        <div class="events-grid" id="eventsGrid"></div>
    </div>

    <!-- M3U আপলোড -->
    <div class="section" id="m3u-upload">
        <h2 class="section-title">📤 Import M3U Playlist</h2>
        <div class="upload-box" onclick="document.getElementById('m3uFileInput').click()">
            <div class="upload-icon">📁</div>
            <div class="upload-text">Click to upload M3U file</div>
            <button class="upload-btn" type="button">Browse Files</button>
            <input type="file" id="m3uFileInput" accept=".m3u,.m3u8" style="display:none">
        </div>
    </div>

    <!-- চ্যানেল -->
    <div class="section" id="channels">
        <h2 class="section-title">📺 Live Channels</h2>
        
        <div class="category-tabs" id="categoryTabs">
            <button class="category-tab active" data-category="all">All</button>
            <button class="category-tab" data-category="bangladeshi">Bangladeshi</button>
            <button class="category-tab" data-category="kolkata">Kolkata</button>
            <button class="category-tab" data-category="indian">Indian</button>
            <button class="category-tab" data-category="cricket">Cricket</button>
            <button class="category-tab" data-category="football">Football</button>
            <button class="category-tab" data-category="kids">Kids</button>
            <button class="category-tab" data-category="religion">Religion</button>
            <button class="category-tab" data-category="m3u">M3U Playlists</button>
        </div>
        
        <div class="channels-grid" id="channelsGrid"></div>
    </div>

    <!-- প্লেয়ার মডাল -->
    <div class="player-modal" id="playerModal">
        <div class="player-container">
            <div class="player-header">
                <div class="player-title">
                    <span>📺</span>
                    <span id="currentChannelName">Channel</span>
                </div>
                <button class="close-player" onclick="closePlayer()">×</button>
            </div>
            <div class="video-wrapper">
                <video id="videoPlayer" controls autoplay playsinline></video>
                <div class="player-brand-overlay">
                    <img src="https://avatars.githubusercontent.com/u/192325222?v=4" alt="Brand" class="player-brand-logo">
                    <span class="player-brand-text">𝕃𝕚𝕧𝕖 𝕊𝕥𝕣𝕖𝕒𝕞</span>
                </div>
            </div>
            <div class="player-controls">
                <button class="control-btn" onclick="togglePIP()">📱 PIP</button>
                <div class="quality-selector">
                    <button class="control-btn" onclick="toggleQuality()">⚙️ Quality</button>
                    <div class="quality-options" id="qualityOptions">
                        <div class="quality-option" onclick="setQuality('auto')">Auto</div>
                        <div class="quality-option" onclick="setQuality('1080p')">1080p</div>
                        <div class="quality-option" onclick="setQuality('720p')">720p</div>
                    </div>
                </div>
                <button class="control-btn" onclick="toggleFullscreen()">🖥️ Fullscreen</button>
            </div>
        </div>
    </div>

    <!-- ফুটার -->
    <footer>
        <div class="footer-content">
            <div class="footer-section">
                <h3>About Us</h3>
                <p class="footer-text">Premium Live TV with HD & 4K channels. Watch anytime, anywhere.</p>
            </div>
            <div class="footer-section">
                <h3>Follow Us</h3>
                <div class="social-links">
                    <a href="https://facebook.com" target="_blank" class="social-btn">📘 Facebook</a>
                    <a href="https://youtube.com" target="_blank" class="social-btn">📺 YouTube</a>
                    <a href="https://t.me/channel" target="_blank" class="social-btn">✈️ Telegram</a>
                </div>
            </div>
        </div>
        <div class="copyright">
            <p>© 2024 𝕃𝕚𝕧𝕖 𝕊𝕥𝕣𝕖𝕒𝕞. All rights reserved.</p>
        </div>
    </footer>

    <!-- JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/hls.js@latest"></script>
    <script>
        console.log('🚀 Script Loading...');

        // ==========================================
        // ডাটা
        // ==========================================
        
        const ACCOUNTS = {
            'basic_user': {
                password: 'basic123',
                type: 'basic',
                name: 'Basic User',
                email: 'basic@livestream.com',
                expiry: new Date('2024-12-31'),
                allowedChannels: ['b']
            },
            'pro_user': {
                password: 'pro456',
                type: 'pro',
                name: 'Pro User',
                email: 'pro@livestream.com',
                expiry: new Date('2025-06-30'),
                allowedChannels: ['b', 'pro']
            },
            'ultra_user': {
                password: 'ultra789',
                type: 'ultra',
                name: 'Ultra Pro User',
                email: 'ultra@livestream.com',
                expiry: new Date('2025-12-31'),
                allowedChannels: ['b', 'pro', 'pro+']
            }
        };

        const LIVE_EVENTS = [
            {
                match: 'Bangladesh vs India',
                sport: 'Cricket',
                time: '7:00 PM',
                url: 'https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8'
            },
            {
                match: 'Real Madrid vs Barcelona',
                sport: 'Football',
                time: '9:00 PM',
                url: 'https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8'
            }
        ];

        const CHANNELS = [
            {
                name: 'ATN Bangla',
                logo: 'https://upload.wikimedia.org/wikipedia/en/5/53/ATN_Bangla.svg',
                url: 'https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8',
                category: 'bangladeshi',
                badges: ['live', 'hd', 'pro'],
                accountType: 'pro'
            },
            {
                name: 'Channel i',
                logo: 'https://upload.wikimedia.org/wikipedia/en/8/88/Channel-i.svg',
                url: 'https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8',
                category: 'bangladeshi',
                badges: ['live', 'hd', 'b'],
                accountType: 'b'
            },
            {
                name: 'Star Jalsha',
                logo: 'https://upload.wikimedia.org/wikipedia/en/d/d0/Star_Jalsha_logo.jpg',
                url: 'https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8',
                category: 'kolkata',
                badges: ['live', 'hd', 'pro'],
                accountType: 'pro'
            },
            {
                name: 'Sony TV',
                logo: 'https://upload.wikimedia.org/wikipedia/commons/e/ee/Sony_TV_new.png',
                url: 'https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8',
                category: 'indian',
                badges: ['live', 'hd', '4k', 'pro+'],
                accountType: 'pro+'
            },
            {
                name: 'Star Sports',
                logo: 'https://upload.wikimedia.org/wikipedia/en/1/11/Star_Sports_logo.png',
                url: 'https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8',
                category: 'cricket',
                badges: ['live', 'hd', '4k', 'pro+'],
                accountType: 'pro+'
            },
            {
                name: 'Disney Channel',
                logo: 'https://upload.wikimedia.org/wikipedia/commons/d/d2/2019_Disney_Channel_logo.svg',
                url: 'https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8',
                category: 'kids',
                badges: ['live', 'free', 'b'],
                accountType: 'b'
            }
        ];

        let currentUser = null;
        let currentCategory = 'all';
        let currentSlide = 0;
        let m3uChannels = [];
        let hlsInstance = null;

        // ==========================================
        // পেজ লোড
        // ==========================================
        
        window.addEventListener('load', function() {
            console.log('✅ Page Loaded');
            
            checkLoginStatus();
            displayLiveEvents();
            displayChannels();
            startAutoSlide();
            initCategoryTabs();
            initM3UUpload();
            
            setTimeout(() => {
                document.getElementById('loadingScreen').classList.add('hidden');
            }, 1500);
        });

        // ==========================================
        // লগইন চেক
        // ==========================================
        
        function checkLoginStatus() {
            const savedUser = localStorage.getItem('currentUser');
            if (savedUser) {
                const userData = JSON.parse(savedUser);
                const account = ACCOUNTS[userData.username];
                
                if (account && new Date() < new Date(account.expiry)) {
                    currentUser = { username: userData.username, ...account };
                    showUserProfile();
                    document.getElementById('loginModal').classList.add('hidden');
                    console.log('✅ User Logged In:', currentUser.name);
                } else {
                    localStorage.removeItem('currentUser');
                }
            }
        }

        // ==========================================
        // লগইন ফর্ম
        // ==========================================
        
        document.getElementById('loginForm').addEventListener('submit', function(e) {
            e.preventDefault();
            console.log('📝 Login Attempt');
            
            const username = document.getElementById('username').value.trim();
            const password = document.getElementById('password').value;
            const errorDiv = document.getElementById('loginError');
            
            if (ACCOUNTS[username] && ACCOUNTS[username].password === password) {
                const account = ACCOUNTS[username];
                
                if (new Date() > new Date(account.expiry)) {
                    errorDiv.textContent = '⚠️ Account expired!';
                    errorDiv.classList.add('show');
                    setTimeout(() => errorDiv.classList.remove('show'), 3000);
                    return;
                }
                
                currentUser = { username: username, ...account };
                localStorage.setItem('currentUser', JSON.stringify({ username: username }));
                
                showUserProfile();
                document.getElementById('loginModal').classList.add('hidden');
                this.reset();
                
                console.log('✅ Login Success:', currentUser.name);
            } else {
                errorDiv.textContent = '❌ Invalid credentials!';
                errorDiv.classList.add('show');
                setTimeout(() => errorDiv.classList.remove('show'), 3000);
            }
        });

        // ==========================================
        // প্রোফাইল দেখান
        // ==========================================
        
        function showUserProfile() {
            document.getElementById('profileContainer').classList.remove('hidden');
            
            const avatar = currentUser.name.charAt(0).toUpperCase();
            document.getElementById('profileAvatar').textContent = avatar;
            document.getElementById('profileName').textContent = currentUser.name.split(' ')[0];
            
            document.getElementById('dropdownName').textContent = currentUser.name;
            document.getElementById('dropdownEmail').textContent = currentUser.email;
            
            const badge = document.getElementById('dropdownBadge');
            badge.textContent = currentUser.type.toUpperCase();
            badge.className = 'profile-badge badge-' + currentUser.type;
            
            const days = Math.ceil((new Date(currentUser.expiry) - new Date()) / (1000*60*60*24));
            document.getElementById('dropdownExpiry').textContent = `Expires in: ${days} days`;
            
            displayChannels();
        }

        // ==========================================
        // প্রোফাইল টগল
        // ==========================================
        
        function toggleProfile() {
            const dropdown = document.getElementById('profileDropdown');
            dropdown.classList.toggle('active');
        }

        // ==========================================
        // লগআউট
        // ==========================================
        
        function logout() {
            if (confirm('Logout?')) {
                localStorage.removeItem('currentUser');
                currentUser = null;
                document.getElementById('profileContainer').classList.add('hidden');
                closePlayer();
                location.reload();
            }
        }

        // ==========================================
        // ইভেন্ট দেখান
        // ==========================================
        
        function displayLiveEvents() {
            const grid = document.getElementById('eventsGrid');
            grid.innerHTML = '';
            
            LIVE_EVENTS.forEach(event => {
                const card = document.createElement('div');
                card.className = 'event-card';
                card.innerHTML = `
                    <div class="event-header">
                        <div class="event-live-badge">
                            <div class="event-live-dot"></div>
                            LIVE
                        </div>
                        <span style="color: rgba(255,255,255,0.8);">${event.sport}</span>
                    </div>
                    <div class="event-match">${event.match}</div>
                    <div class="event-time">⏰ ${event.time}</div>
                `;
                card.onclick = () => playChannel(event.match, event.url);
                grid.appendChild(card);
            });
        }

        // ==========================================
        // চ্যানেল দেখান
        // ==========================================
        
        function displayChannels() {
            const grid = document.getElementById('channelsGrid');
            grid.innerHTML = '';
            
            let channels = [...CHANNELS, ...m3uChannels];
            
            if (currentCategory === 'm3u') {
                channels = m3uChannels;
            } else if (currentCategory !== 'all') {
                channels = channels.filter(ch => ch.category === currentCategory);
            }
            
            if (currentUser) {
                channels = channels.filter(ch => {
                    if (!ch.accountType) return true;
                    return currentUser.allowedChannels.includes(ch.accountType);
                });
            }
            
            console.log(`📺 Showing ${channels.length} channels`);
            
            channels.forEach(channel => {
                const card = document.createElement('div');
                card.className = 'channel-card';
                
                const badges = channel.badges?.map(b => 
                    `<span class="badge badge-${b}">${b}</span>`
                ).join('') || '';
                
                card.innerHTML = `
                    <div class="channel-logo-container">
                        <img src="${channel.logo}" alt="${channel.name}" class="channel-logo" 
                             onerror="this.src='https://via.placeholder.com/200?text=${encodeURIComponent(channel.name)}'">
                    </div>
                    <div class="channel-name">${channel.name}</div>
                    <div class="channel-badges">${badges}</div>
                `;
                
                card.onclick = () => playChannel(channel.name, channel.url);
                grid.appendChild(card);
            });
            
            if (channels.length === 0) {
                grid.innerHTML = '<p style="color:white; text-align:center; padding:3rem; grid-column:1/-1;">No channels available</p>';
            }
        }

        // ==========================================
        // ক্যাটাগরি ট্যাব ইনিট
        // ==========================================
        
        function initCategoryTabs() {
            const tabs = document.querySelectorAll('.category-tab');
            tabs.forEach(tab => {
                tab.addEventListener('click', function() {
                    tabs.forEach(t => t.classList.remove('active'));
                    this.classList.add('active');
                    currentCategory = this.dataset.category;
                    displayChannels();
                    console.log('📂 Category:', currentCategory);
                });
            });
        }

        // ==========================================
        // চ্যানেল প্লে
        // ==========================================
        
        function playChannel(name, url) {
            if (!currentUser) {
                alert('Please login first!');
                return;
            }
            
            console.log('▶️ Playing:', name);
            
            document.getElementById('playerModal').classList.add('active');
            document.getElementById('currentChannelName').textContent = name;
            
            const video = document.getElementById('videoPlayer');
            
            if (hlsInstance) {
                hlsInstance.destroy();
            }
            
            if (url.includes('.m3u8')) {
                if (Hls.isSupported()) {
                    hlsInstance = new Hls();
                    hlsInstance.loadSource(url);
                    hlsInstance.attachMedia(video);
                    hlsInstance.on(Hls.Events.MANIFEST_PARSED, () => {
                        video.play().catch(e => console.log('Autoplay blocked'));
                    });
                } else if (video.canPlayType('application/vnd.apple.mpegurl')) {
                    video.src = url;
                    video.play();
                }
            } else {
                video.src = url;
                video.play();
            }
        }

        // ==========================================
        // প্লেয়ার বন্ধ
        // ==========================================
        
        function closePlayer() {
            document.getElementById('playerModal').classList.remove('active');
            const video = document.getElementById('videoPlayer');
            video.pause();
            video.src = '';
            
            if (hlsInstance) {
                hlsInstance.destroy();
                hlsInstance = null;
            }
        }

        // ==========================================
        // PIP
        // ==========================================
        
        function togglePIP() {
            const video = document.getElementById('videoPlayer');
            if (document.pictureInPictureElement) {
                document.exitPictureInPicture();
            } else {
                video.requestPictureInPicture().catch(e => alert('PIP not supported'));
            }
        }

        // ==========================================
        // ফুলস্ক্রিন
        // ==========================================
        
        function toggleFullscreen() {
            const container = document.querySelector('.player-container');
            if (!document.fullscreenElement) {
                container.requestFullscreen();
            } else {
                document.exitFullscreen();
            }
        }

        // ==========================================
        // কোয়ালিটি
        // ==========================================
        
        function toggleQuality() {
            document.getElementById('qualityOptions').classList.toggle('active');
        }

        function setQuality(q) {
            console.log('Quality:', q);
            toggleQuality();
        }

        // ==========================================
        // M3U আপলোড
        // ==========================================
        
        function initM3UUpload() {
            document.getElementById('m3uFileInput').addEventListener('change', function(e) {
                const file = e.target.files[0];
                if (!file) return;
                
                const reader = new FileReader();
                reader.onload = function(ev) {
                    parseM3U(ev.target.result);
                };
                reader.readAsText(file);
            });
        }

        function parseM3U(content) {
            const lines = content.split('\n');
            m3uChannels = [];
            let current = {};
            
            lines.forEach(line => {
                line = line.trim();
                if (line.startsWith('#EXTINF:')) {
                    const nameMatch = line.match(/,(.+)$/);
                    const logoMatch = line.match(/tvg-logo="(.+?)"/);
                    current = {
                        name: nameMatch ? nameMatch[1] : 'Unknown',
                        logo: logoMatch ? logoMatch[1] : 'https://via.placeholder.com/200',
                        category: 'm3u',
                        badges: ['live', 'free']
                    };
                } else if (line && !line.startsWith('#')) {
                    current.url = line;
                    m3uChannels.push(current);
                    current = {};
                }
            });
            
            alert(`✅ Imported ${m3uChannels.length} channels!`);
            console.log('📁 M3U Channels:', m3uChannels.length);
            
            // M3U ট্যাব সিলেক্ট করুন
            const m3uTab = document.querySelector('[data-category="m3u"]');
            if (m3uTab) m3uTab.click();
        }

        // ==========================================
        // স্লাইডার
        // ==========================================
        
        function changeSlide(direction) {
            const slides = document.querySelectorAll('.slide');
            const dots = document.querySelectorAll('.dot');
            
            slides[currentSlide].classList.remove('active');
            dots[currentSlide].classList.remove('active');
            
            currentSlide = (currentSlide + direction + slides.length) % slides.length;
            
            slides[currentSlide].classList.add('active');
            dots[currentSlide].classList.add('active');
        }

        function goToSlide(index) {
            const slides = document.querySelectorAll('.slide');
            const dots = document.querySelectorAll('.dot');
            
            slides[currentSlide].classList.remove('active');
            dots[currentSlide].classList.remove('active');
            
            currentSlide = index;
            
            slides[currentSlide].classList.add('active');
            dots[currentSlide].classList.add('active');
        }

        function startAutoSlide() {
            setInterval(() => changeSlide(1), 5000);
        }

        // ==========================================
        // মেনু টগল
        // ==========================================
        
        function toggleMenu() {
            document.getElementById('navLinks').classList.toggle('active');
        }

        // ==========================================
        // স্ক্রল টু টপ
        // ==========================================
        
        function scrollToTop() {
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        // ==========================================
        // ক্লিক আউটসাইড
        // ==========================================
        
        document.addEventListener('click', function(e) {
            const profile = document.querySelector('.profile-btn');
            const dropdown = document.getElementById('profileDropdown');
            if (profile && !profile.contains(e.target)) {
                dropdown.classList.remove('active');
            }
            
            const navLinks = document.getElementById('navLinks');
            const toggle = document.querySelector('.menu-toggle');
            if (!navLinks.contains(e.target) && !toggle.contains(e.target)) {
                navLinks.classList.remove('active');
            }
        });

        // ESC কী
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape') {
                closePlayer();
                document.getElementById('profileDropdown').classList.remove('active');
                document.getElementById('navLinks').classList.remove('active');
            }
        });

        console.log('✅ Script Ready!');
    </script>
</body>
</html>
